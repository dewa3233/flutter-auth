import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/NoConnection/components/body.dart';

class NoConnection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
